{
  "name": "moneymanagement",
  "version": "1.0.0",
  "private": true,
  "description": "Aplicación web de finanzas personales con Google Sheets como backend.",
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check sw.js && node scripts/check-gs.mjs",
    "check:versions": "node scripts/check-versions.mjs",
    "test": "node --test tests/*.test.mjs",
    "lint": "eslint app.js sw.js tests scripts eslint.config.mjs",
    "verify": "npm run check && npm run check:versions && npm run test"
  },
  "devDependencies": {
    "eslint": "^9.13.0"
  }
}
