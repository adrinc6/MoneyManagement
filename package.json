{
  "name": "moneymanagement",
  "version": "1.0.0",
  "private": true,
  "description": "Aplicación web de finanzas personales con Google Sheets como backend.",
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check sw.js",
    "test": "node --test tests/*.test.mjs",
    "lint": "eslint tests sw.js eslint.config.mjs",
    "verify": "npm run check && npm run test"
  },
  "devDependencies": {
    "eslint": "^9.13.0"
  }
}
